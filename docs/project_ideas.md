# Project Ideas & Notes

Use `/note [idea]` to add new items here.
Use `/implementnotes` to select and implement items from this list.

## Backlog

- Add strategy download (JSON file export) and strategy import from JSON file
- Implement hot reload of strategy and watchlist within the bot engine (like complete the current loop, then if reload needed, reload the pushed strategy/ watchlist)
